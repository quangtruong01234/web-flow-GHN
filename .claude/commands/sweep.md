# Weekly Backlog Sweep

Read `.ai/workflows/sweep.md` completely, then execute the sweep.

Treat an optional argument (a number, a risk/handoff item id, `audit [area]`, or
`propose`) as the mode selector per the workflow. Default with no argument: fix
the single highest-priority open item end-to-end.
